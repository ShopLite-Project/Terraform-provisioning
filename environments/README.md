# Environments

Shared environment configuration lives here and can be loaded by the runnable Terraform roots.

The intent is to keep common values in YAML files, similar to the `terraform-modules-dev` pattern.
